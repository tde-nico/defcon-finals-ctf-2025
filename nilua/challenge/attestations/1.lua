-- This is a simple attestation function that adds two numbers and subtracts two numbers
-- and prints the result

function add(x, y)
    return x + y
end

function sub(x, y)
    return x - y
end

return add(100, 200) + sub(400, 300)
